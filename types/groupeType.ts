export interface GroupType {
  id: string; // Notez bien que l'ID ici est une chaîne de caractères ("1", "2")
  name: string;
  image: string;
  rating: number;
  reviews: number; // Nombre d'avis (entier)
}
